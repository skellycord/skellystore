
let reactErrorCodes;

/* async function init() {
    const errorDecoderData = await fetch("https://reactjs.org/page-data/docs/error-decoder.html/page-data.json");
    const { result } = await errorDecoderData.json();
    
    reactErrorCodes = JSON.parse(result.data.errorCodes);
}

init(); */

export default reactErrorCodes;